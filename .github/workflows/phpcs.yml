name: PHPCS

on: [push]

jobs:
  test-phpcs:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - uses: php-actions/composer@v6
	  with:
	    dev: no

    - name: Run PHPCS
      run: vendor/bin/phpcs
